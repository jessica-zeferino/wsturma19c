package decisao;

import javax.swing.JOptionPane;

public class Exercicio3 {

	/*
	 * Exercicio 3:
	 * Receba o valor do salário bruto, o nome de um colaborador e a qtde de faltas.
	 * Se não houver faltas o salario bruto receberá um aumento de 2%.
	 * 
	 * Calcule o valor do INSS sobre o salário bruto.
	 * Exiba no final o salário bruto, e o salário liquido (retirando o INSS e 8% do FGTS)
	 */
	
	public static void main(String[] args) {
		
		String nome = JOptionPane.showInputDialog("Digite o nome do colaborador: ");
		float salario = Float.parseFloat(JOptionPane.showInputDialog("Qual o salário do colaborador? "));
		short faltas = Short.parseShort(JOptionPane.showInputDialog("Digite a quantidade de faltas: "));
		float liquido;
		
		if (faltas==0) {
			salario = salario * (float)1.02;
		}
		
		liquido = salario/(float)1.08;
		
		System.out.println("Olá, " + nome + " . Seu salário é: ");
		System.out.println("Salário bruto: " + salario);
		System.out.println("Salário líquido: " + liquido);
		
	}
	
}
